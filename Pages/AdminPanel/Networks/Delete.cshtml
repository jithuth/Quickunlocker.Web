@page "{id:guid}"
@model Quickunlocker.Web.Pages.AdminPanel.Networks.DeleteModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Delete Network";
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Delete Mobile Network</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/AdminPanel/Index">Home</a></li>
                    <li class="breadcrumb-item"><a href="/AdminPanel/Networks">Networks</a></li>
                    <li class="breadcrumb-item active">Delete</li>
                </ol>
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8">
                <div class="card card-danger">
                    <div class="card-header">
                        <h3 class="card-title">Confirm Deletion</h3>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-warning">
                            <h5><i class="icon fas fa-exclamation-triangle"></i> Warning!</h5>
                            Are you sure you want to delete this mobile network?
                        </div>

                        <dl class="row">
                            <dt class="col-sm-3">Network Name</dt>
                            <dd class="col-sm-9"><strong>@Model.Network.Name</strong></dd>

                            <dt class="col-sm-3">Country</dt>
                            <dd class="col-sm-9">
                                @if (Model.Network.Country != null)
                                {
                                    @if (!string.IsNullOrEmpty(Model.Network.Country.FlagUrl))
                                    {
                                        <img src="@Model.Network.Country.FlagUrl" alt="@Model.Network.Country.Name" style="height:20px; width:auto; margin-right:5px;" />
                                    }
                                    <span>@Model.Network.Country.Name</span>
                                }
                            </dd>

                            <dt class="col-sm-3">Display Order</dt>
                            <dd class="col-sm-9">@Model.Network.DisplayOrder</dd>

                            <dt class="col-sm-3">Status</dt>
                            <dd class="col-sm-9">
                                @if (Model.Network.IsActive)
                                {
                                    <span class="badge badge-success">Enabled</span>
                                }
                                else
                                {
                                    <span class="badge badge-secondary">Disabled</span>
                                }
                            </dd>

                            <dt class="col-sm-3">Created</dt>
                            <dd class="col-sm-9">@Model.Network.CreatedAt.ToString("MMM dd, yyyy HH:mm")</dd>

                            @if (!string.IsNullOrEmpty(Model.Network.LogoUrl))
                            {
                                <dt class="col-sm-3">Logo</dt>
                                <dd class="col-sm-9">
                                    <img src="@Model.Network.LogoUrl" alt="@Model.Network.Name" class="img-thumbnail" style="max-height:60px;" />
                                </dd>
                            }
                        </dl>

                        <div class="callout callout-info">
                            <h5><i class="fas fa-info-circle"></i> Note:</h5>
                            This is a soft delete. The network will be marked as deleted but will remain in the database for record-keeping purposes.
                        </div>
                    </div>
                    
                    <div class="card-footer">
                        <form method="post">
                            <button class="btn btn-danger" type="submit">
                                <i class="fas fa-trash"></i> Yes, Delete Network
                            </button>
                            <a class="btn btn-secondary" href="/AdminPanel/Networks">
                                <i class="fas fa-times"></i> Cancel
                            </a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
